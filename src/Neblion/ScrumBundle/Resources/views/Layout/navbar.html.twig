<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>

            <a class="brand" href="{{ path('neblion_scrum_welcome') }}">Neblion Scrum</a>

            <div class="nav-collapse">
                <ul class="nav">
                    <li><a href="#">{{ 'Help'|trans }}</a></li>
                    <li><a href="#">{{ 'Contact'|trans }}</a></li>
                </ul>

                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <ul class="nav pull-right">
                        <li><a href="{{ path('neblion_scrum_projects') }}">{{ 'Your projects'|trans }}</a></li>
                        <li class="divider-vertical"></li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-user icon-white"></i> {{ app.user.username }} <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="{{ path('profile_edit') }}">{{ 'Edit profile'|trans }}</a></li>
                                <li><a href="{{ path('fos_user_change_password') }}">{{ 'Change password'|trans }}</a></li>
                                <li class="divider"></li>
                                <li><a href="{{ path('fos_user_security_logout') }}">{{ 'Sign Out'|trans }}</a></li>
                            </ul>
                        </li>
                    </ul>
                {% else %}
                    <div class="btn-group pull-right">
                        <a class="btn btn-primary" href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                        <a class="btn btn-success" href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
                    </div>
                {% endif %}
            </div><!--/.nav-collapse -->
        </div><!-- /.container -->
    </div><!-- /.navbar-inner -->
</div><!-- /.navbar -->